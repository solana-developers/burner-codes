/**
 * local storage key name that stores the base58 encoded
 * `secretKey` of the burner wallet
 */
export const LOCAL_STORAGE_BURNER_KEY = "default_keypair";

/**
 * local storage key where user generated claim codes are stored
 */
export const LOCAL_STORAGE_CLAIM_CODE = "claim_codes";

/**
 * base transaction fee for each signature (aka lamports per signer)
 */
export const LAMPORTS_PER_SIGNER = 5_000;
